# 20. 화면별 작업 분배표 (Figma 리디자인)

Figma 파일(`xBf3b09D6Bj1dTiCixt25e`, "Application UI" 페이지)의 프로토타입 연결까지 마친 상태를 기준으로, 실제 코드 리디자인을 나눠서 진행하기 위한 표다. 작업 방법은 `.claude/skills/figma-to-mealchat-screen/SKILL.md`를 따른다.

> **범위 제외**: `1_로그인`, `1_회원가입`, `1.5_취향게임`의 온보딩 진입부(카카오·Apple·Google 소셜로그인, 이메일 가입 4단계, 가입 중 취향게임 진입) — 기존 `AuthScreen.tsx`를 그대로 유지한다. 단, 취향게임 자체는 프로필 탭의 "게임 시작"에서도 재진입하는 기존 기능(`ProfileSetup.tsx`)이라 아래 표에 별도로 포함했다.

## 담당자 배정 기준

2인 분업, **파일 단위로 나눠서 충돌·의존성을 최소화**했다:

- **담당자 A** — `App.tsx` 및 그 인라인 로직 쪽: 네비게이션 통합, 홈, 채팅방 전체 (12개 항목)
- **담당자 B** — `ProfileSetup.tsx`/`ScheduleGrid.tsx` 쪽: 프로필, 일정 조율 전체 (11개 항목, "프로필을 탭으로 전환"은 A의 네비게이션 통합이 끝난 뒤 시작)

> **2026-08-13 변경**: 담당자 A 가 12개 항목을 끝낸 뒤 **B 범위까지 이어서 맡게 되었다.** 파일 분리로 인한 충돌 걱정은 더 이상 없다.

두 사람이 겹치는 파일이 거의 없어 병렬 작업 중 merge 충돌 위험이 낮다. 유일한 순서 의존성은 "네비게이션 4탭 통합"(A) → "프로필을 탭으로 전환"(B) 하나뿐.

## 0. 선행 작업 (다른 화면 작업의 전제조건)

| 항목 | 대상 파일 | 작업 내용 | 담당자 | 상태 |
|---|---|---|---|---|
| 네비게이션 4탭 통합 | `src/contexts/NavigationContext.tsx`, `src/App.tsx` | `activeTab` 타입을 `'home'\|'schedule'\|'chat'\|'profile'`로 확장, 인라인 탭바를 `<BottomNav>`로 교체 | 담당자 A | 완료 |
| 홈 탭 콘텐츠 설계 | `src/screens/HomeTab.tsx` (신규) | Figma `2_홈/홈/메인` 참고 — "다가올 일정" 목록 + 일정 잡기 CTA. 기존 코드에 대응 화면 없음 | 담당자 A | 완료 |
| 프로필을 탭으로 전환 | `src/App.tsx` | 설정 모달(`showSettingsModal`)을 걷어내고 `activeTab === 'profile'` 에서 `ProfileSetup` 을 직렬 렌더. 스와이프 뒤로가기는 `profilePanResponder` 로 옮겨 하위 뷰(수정/취향게임)만 닫는다 | 담당자 B | 완료 |

기반 컴포넌트(`Button`/`BottomNav`/`AppHeader`/`theme.ts` 색상)는 이미 완료됨 — 이 표의 나머지 항목들은 그 위에서 작업.

> **2026-08-13 보정**: `AppHeader` 는 만들어져 있었지만 **어디에서도 쓰이지 않아** 화면 상단이 리스킨 전 그대로였다(밀챗 로고 + 종 + 톱니). `App.tsx` 의 인라인 헤더를 `<AppHeader>` 로 교체했고, 방 안에서는 그리지 않는다 — Figma 는 같은 자리에서 `AppHeader` 와 `roomHeader` 가 서로를 대체하는데 둘이 겹쳐 그려지고 있었다. `BottomNav` 도 Figma 와 달랐다(활성 표시 바 없음 / 비활성 라벨이 회색 / 그림자 없음) — 셋 다 맞췄다.

## 1. 홈

| Figma 화면 | 기존 파일 | 비고 | 담당자 | 상태 |
|---|---|---|---|---|
| 홈/메인 (`309:1064`) | `src/screens/HomeTab.tsx` (위 0번 항목과 동일) | 프로모션 배너는 컴포넌트만 구현됨 — 배너를 내려줄 데이터 소스가 없어 `promos` 를 넘기지 않으므로 현재는 렌더되지 않음 | 담당자 A | 리뷰 대기 |
| 홈/알림 (`549:3408`) | `App.tsx`의 알림 드로어(`showNotifications`) | **판단 완료: 드로어 유지.** Figma도 전용 화면이 아니라 Dim + 오버레이 패널(196×350)임. Dim·둥근 패널·`모두 읽음`·Figma 카드 톤·"최근 30일의 알림을 보여드려요" 푸터 반영. **미반영**: 필터 칩(전체/일정/정산/메이트)과 행별 읽음 테두리 — 알림에 분류도 개별 읽음 상태도 없다(`showNotificationsRedDot` 플래그 하나뿐) | 담당자 A | 리뷰 대기 |

## 2. 프로필

| Figma 화면 | 기존 파일 | 비고 | 담당자 | 상태 |
|---|---|---|---|---|
| 프로필/프로필 홈 | `ProfileSetup.tsx` (`activeSubTab==='profile'` + `'settings'` 허브, `view`가 아님 — 주의) | ✅ 리스킨 완료 | 담당자 B | 완료 |
| 프로필/프로필 수정 | `ProfileSetup.tsx` (`activeSettingSection==='profile'`) | ✅ 저장 버튼 Button 컴포넌트로 교체 완료 | 담당자 B | 완료 |
| 프로필/지도 위치 지정 | `ProfileSetup.tsx` 내 지도 모달(`showMapModal`) | `LocationPickerModal.tsx`는 별개 파일(방 관련), 여기서 안 씀 — ✅ 완료 | 담당자 B | 완료 |
| 프로필/정보 공개 범위 | `ProfileSetup.tsx` (`activeSettingSection==='privacy'`) | ✅ 완료 (원래도 THEME 일관성 높았음) | 담당자 B | 완료 |
| 취향게임/고기·해산물·양식·중식·일식·한식 | `ProfileSetup.tsx` 스와이프 퀴즈 (`PanResponder`) | ✅ 완료 — 가장 하드코딩 많던 영역 | 담당자 B | 완료 |

## 3. 채팅방

| Figma 화면 | 기존 파일 | 비고 | 담당자 | 상태 |
|---|---|---|---|---|
| 채팅방/홈 (목록) (`549:3507`) | `RoomListView.tsx`, `RoomCard.tsx` | 방 카드에 필요한 멤버 아바타/마지막 메시지가 `rooms` 에 없어 `fetchRoomSummaries`(참여자 1회 + 메시지 1회)를 추가하고 `RoomContext.roomSummaries` 로 내려준다. **Figma와 다른 점 2가지**: ① N빵 정산 대장 카드는 Figma에 없지만 정산의 유일한 상시 진입점이라 톤만 맞춰 남김 ② 헤더의 "새 약속 만들기"를 카드의 + 버튼으로 옮김 | 담당자 A | 리뷰 대기 |
| 채팅방/방 상세정보 (`159:604`) | `RoomInfoModal.tsx` | 가운데 띄우는 모달 → 전체 화면 슬라이드 시트 + Figma `roomHeader`. **Figma에 없어 살려 둔 기능**: 약속 이름 변경 / 약속 일시 / 테마 색상 / 멤버 추방(같은 카드 언어로 옮김). **Figma에서 가져온 것**: 방 나가기 버튼을 채팅방 인라인 바에서 이 화면으로 이동. **미반영**: Figma 프레임의 하단 BottomNav(모달이라 안 그림), roomHeader의 ⋮ 메뉴(방 나가기가 본문에 있어 생략) | 담당자 A | 리뷰 대기 |
| 채팅/채팅방 (메시지 화면) (`315:4324`) | `src/screens/RoomChatView.tsx` (신규, `App.tsx` 인라인에서 분리) | 이모티콘 표는 `src/constants/emoticons.ts` 로 분리. 하단 패널 버튼 4개(일정·메뉴·정산·멤버)를 추가 — 전부 `roomOverlay` 로 공통 바텀시트를 연다. **미반영**: 시스템 메시지 pill, 인챗 "일정이 확정됐어요" 카드(둘 다 메시지 타입 모델이 없음), 입력창 왼쪽 첨부(+) 버튼(기능 없음) | 담당자 A | 리뷰 대기 |
| 채팅/일정 패널 (오버레이) (`553:408`) | `RoomScheduleSheet.tsx`, `roomOverlay==='schedule'` | 공통 `RoomPanelSheet` 로 감쌌고 진행률/확정 카드를 리스킨. **Figma와 다른 점**: Figma는 화면 60% 높이의 낮은 시트에 날짜 칩 + 시간 후보 목록이지만, 그 압축 형태로 바꾸려면 `ScheduleGrid`(담당자 B, 6227줄)를 손대야 해서 범위 밖. 대신 `expanded` 로 전체 높이를 쓴다 — ScheduleGrid는 최소 600dp가 필요하고 높이가 모자라면 0으로 접힌 전례(커밋 4ea5a02)가 있음 | 담당자 A | 리뷰 대기 |
| 채팅/메뉴 패널 (오버레이) (`553:698`) | `RoomMenuTab.tsx`, `BaeminSurvey.tsx` | 공통 `RoomPanelSheet` 안으로 들어가고 투표 행을 Figma Option 행으로 리스킨(득표 표기 `N명`→`N표`). Figma에 없는 기존 기능(AI 추천 메뉴 / 참여자별 선택 / 메뉴 룰렛 / 배민 설문)은 그대로 둠 | 담당자 A | 리뷰 대기 |
| 채팅/정산 패널 (오버레이) (`553:727`) | `src/screens/RoomDutchPaySheet.tsx` (신규) | **결정(2026-08-13) 반영: 방 안에서는 Figma대로 정산 1건.** 방의 가장 최근 청구서 1건만 다루고 총액/1인당·참여자별 완료·미납·영수증 자동 입력·요청 보내기로 좁혔다. `DutchPay.tsx`(2240줄)는 손대지 않았다 — 방 밖의 "나의 N빵 정산 대장"이 여러 방·여러 건을 보여줘야 해서 그 구조가 계속 필요하다. **DB 제약은 걸지 않았다**: 방당 1건은 화면에서 지키는 규칙이고, 이미 여러 건인 방은 최근 것을 보여준다. 영수증 OCR은 총액만 읽는 형태로 축소(기존 품목별 OCR은 `DutchPay.tsx`에 남음) | 담당자 A | 리뷰 대기 |
| 채팅/멤버 패널 (오버레이) (`553:768`) | `src/screens/RoomMemberSheet.tsx` (신규) | 채팅에서 바로 여는 패널로 분리(방 상세정보의 목록은 그대로 유지). **미반영**: Figma의 "온라인 / 3시간 전" 접속 상태 줄 — presence 데이터가 없음 | 담당자 A | 리뷰 대기 |
| 채팅/채팅방/이모티콘 (오버레이) (`555:416`) | `RoomChatView.tsx` 의 이모티콘 패널 | 이름표를 떼고 4열 타일 그리드로 리스킨. **미반영**: Figma의 "전체 보기 →" — 패널이 이미 17개 전부를 스크롤로 보여줘서 갈 곳이 없음(대신 개수를 표시) | 담당자 A | 리뷰 대기 |

## 4. 일정 조율

⚠️ **조사 결과 Figma와 실제 구조가 상당히 어긋나 있음** (담당자 B, 다음 세션에서 착수 예정). 순수 리스킨(기존 구조 유지, 색상/버튼만 교체)으로 접근하기로 결정함 — 흐름 재설계는 하지 않음.

| Figma 화면 | 기존 파일 | 비고 | 담당자 | 상태 |
|---|---|---|---|---|
| 일정 조율 (허브) | `ScheduleTab.tsx` → `ScheduleGrid.tsx` (실제로는 `isCoordination=true` 모드로 렌더됨, `isGlobal` 아님 — 관련 브랜치만 손댈 것) | 메이트선택(3826-3951)+월간그리드(4032-4093)+AI날짜추천(4162-4299)+미드포인트추천(4301-4485, THEME 전혀 안 씀)+방생성모달(4515-4618). 6227줄 — 섹션 단위로 접근 | 담당자 B | 시작 전 |
| 일정 조율/일정 추가/디테일 선택 | 대응 화면 없음 — 허브의 방생성모달(이름+장소만)과 별개로 `CreateRoomModal.tsx`(이름→날짜→메이트 3단계, App.tsx의 다른 진입점)가 따로 존재 | 순수 리스킨 결정: 허브의 기존 방생성모달을 리스킨, 구조 재설계는 안 함 | 담당자 B | 시작 전 |
| 일정 조율/일정 추가/시간 선택 | `CalendarModal.tsx` (24시간 단위 드래그 — `ScheduleGrid.tsx`의 30분 그리드 `renderTimeSlotSelector`는 coordination 모드에서 죽은 코드, 안 씀) | 이 파일이 색상 하드코딩 제일 심함(거의 전체가 슬레이트 다크 팔레트) | 담당자 B | 시작 전 |
| 일정 조율/일정 추가/AI 추천 TOP3 | `AIRecommendModal.tsx` (별도 파일! `ScheduleGrid.tsx` 안에 없음) | 데이터는 `aiRecommender.ts`의 `calculateAIRecommendations`. 순위별 색상(`#8b5cf6`/`#3b82f6`/`#64748b`)이 하드코딩 | 담당자 B | 시작 전 |
| 일정 조율/일정 추가/확정 | `ScheduleGrid.tsx`(3792-3823, coordination에선 원래 안 뜸) + `RoomScheduleSheet.tsx`(162-178, `THEME.confirmed` 보라색으로 별도 확정 박스) | 통일된 "확정됐어요" 성공 화면 자체가 코드에 없음(신규 UI 필요) — 순수 리스킨 결정에 따라 이번엔 두 군데 색상만 정리, 화면 통합은 안 함 | 담당자 B | 시작 전 |

## 상태 값

`시작 전` → `진행중` → `리뷰 대기` → `완료`
