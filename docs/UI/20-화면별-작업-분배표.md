# 20. 화면별 작업 분배표 (Figma 리디자인)

Figma 파일(`xBf3b09D6Bj1dTiCixt25e`, "Application UI" 페이지)의 프로토타입 연결까지 마친 상태를 기준으로, 실제 코드 리디자인을 나눠서 진행하기 위한 표다. 작업 방법은 `.claude/skills/figma-to-mealchat-screen/SKILL.md`를 따른다.

> **범위 제외**: `1_로그인`, `1_회원가입`, `1.5_취향게임`의 온보딩 진입부(카카오·Apple·Google 소셜로그인, 이메일 가입 4단계, 가입 중 취향게임 진입) — 기존 `AuthScreen.tsx`를 그대로 유지한다. 단, 취향게임 자체는 프로필 탭의 "게임 시작"에서도 재진입하는 기존 기능(`ProfileSetup.tsx`)이라 아래 표에 별도로 포함했다.

## 담당자 배정 기준

2인 분업, **파일 단위로 나눠서 충돌·의존성을 최소화**했다:

- **담당자 A** — `App.tsx` 및 그 인라인 로직 쪽: 네비게이션 통합, 홈, 채팅방 전체 (12개 항목)
- **담당자 B** — `ProfileSetup.tsx`/`ScheduleGrid.tsx` 쪽: 프로필, 일정 조율 전체 (11개 항목, "프로필을 탭으로 전환"은 A의 네비게이션 통합이 끝난 뒤 시작)

두 사람이 겹치는 파일이 거의 없어 병렬 작업 중 merge 충돌 위험이 낮다. 유일한 순서 의존성은 "네비게이션 4탭 통합"(A) → "프로필을 탭으로 전환"(B) 하나뿐.

## 0. 선행 작업 (다른 화면 작업의 전제조건)

| 항목 | 대상 파일 | 작업 내용 | 담당자 | 상태 |
|---|---|---|---|---|
| 네비게이션 4탭 통합 | `src/contexts/NavigationContext.tsx`, `src/App.tsx` | `activeTab` 타입을 `'home'\|'schedule'\|'chat'\|'profile'`로 확장, 인라인 탭바를 `<BottomNav>`로 교체 | 담당자 A | 완료 |
| 홈 탭 콘텐츠 설계 | `src/screens/HomeTab.tsx` (신규) | Figma `2_홈/홈/메인` 참고 — "다가올 일정" 목록 + 일정 잡기 CTA. 기존 코드에 대응 화면 없음 | 담당자 A | 완료 |
| 프로필을 탭으로 전환 | `src/App.tsx` `handleTabChange` 의 `'profile'` 분기 + 설정 모달, `ProfileSetup.tsx` | 현재 모달(`showSettingsModal`)로만 열리는 프로필을 "프로필" 탭에서 모달 없이 `'main'` 뷰 직렬 렌더. A가 임시로 `handleTabChange('profile') → setShowSettingsModal(true)` 브리지를 넣어 뒀으니 그 분기를 지우고 대체할 것 (`TODO(담당자 B)` 주석 표시됨) | 담당자 B | **시작 가능** |

기반 컴포넌트(`Button`/`BottomNav`/`AppHeader`/`theme.ts` 색상)는 이미 완료됨 — 이 표의 나머지 항목들은 그 위에서 작업.

## 1. 홈

| Figma 화면 | 기존 파일 | 비고 | 담당자 | 상태 |
|---|---|---|---|---|
| 홈/메인 (`309:1064`) | `src/screens/HomeTab.tsx` (위 0번 항목과 동일) | 프로모션 배너는 컴포넌트만 구현됨 — 배너를 내려줄 데이터 소스가 없어 `promos` 를 넘기지 않으므로 현재는 렌더되지 않음 | 담당자 A | 리뷰 대기 |
| 홈/알림 (`549:3408`) | `App.tsx`의 알림 드로어(`showNotifications`) | **판단 완료: 드로어 유지.** Figma도 전용 화면이 아니라 Dim + 오버레이 패널(196×350)임 | 담당자 A | 시작 전 |

## 2. 프로필

| Figma 화면 | 기존 파일 | 비고 | 담당자 | 상태 |
|---|---|---|---|---|
| 프로필/프로필 홈 | `ProfileSetup.tsx` (`'main'` 뷰) | | 담당자 B | 시작 전 |
| 프로필/프로필 수정 | `ProfileSetup.tsx` (`'edit'` 뷰) | | 담당자 B | 시작 전 |
| 프로필/지도 위치 지정 | `LocationPickerModal.tsx`, `ProfileSetup.tsx` 지도 선택부 | `SafeMapView` 재사용 | 담당자 B | 시작 전 |
| 프로필/정보 공개 범위 | `ProfileSetup.tsx` privacy 섹션 | | 담당자 B | 시작 전 |
| 취향게임/고기·해산물·양식·중식·일식·한식 | `ProfileSetup.tsx` 스와이프 퀴즈 (`PanResponder`) | 로직 기존 구현 있음, 카드 UI만 리스킨 | 담당자 B | 시작 전 |

## 3. 채팅방

| Figma 화면 | 기존 파일 | 비고 | 담당자 | 상태 |
|---|---|---|---|---|
| 채팅방/홈 (목록) | `RoomListView.tsx`, `RoomCard.tsx` | | 담당자 A | 시작 전 |
| 채팅방/방 상세정보 | `RoomInfoModal.tsx` | | 담당자 A | 시작 전 |
| 채팅/채팅방 (메시지 화면) | `App.tsx:3294-3624` (인라인) | 별도 파일 분리 권장 | 담당자 A | 시작 전 |
| 채팅/일정 패널 (오버레이) | `RoomScheduleSheet.tsx`, `roomOverlay==='schedule'` | | 담당자 A | 시작 전 |
| 채팅/메뉴 패널 (오버레이) | `RoomMenuTab.tsx`, `BaeminSurvey.tsx` | | 담당자 A | 시작 전 |
| 채팅/정산 패널 (오버레이) | `DutchPay.tsx` | ⚠️ 구조 차이 큼 — SKILL.md §5 먼저 읽을 것 | 담당자 A | 시작 전 |
| 채팅/멤버 패널 (오버레이) | `RoomInfoModal.tsx` 멤버 목록부 | | 담당자 A | 시작 전 |
| 채팅/채팅방/이모티콘 (오버레이) | `App.tsx` 이모티콘 그리드 토글 | | 담당자 A | 시작 전 |

## 4. 일정 조율

| Figma 화면 | 기존 파일 | 비고 | 담당자 | 상태 |
|---|---|---|---|---|
| 일정 조율 (허브) | `ScheduleTab.tsx` → `ScheduleGrid.tsx` | 6227줄 — 섹션 단위로 접근 | 담당자 B | 시작 전 |
| 일정 조율/일정 추가/디테일 선택 | `ScheduleGrid.tsx` 내부 상태 | 독립 화면으로 분리할지 판단 필요 | 담당자 B | 시작 전 |
| 일정 조율/일정 추가/시간 선택 | `ScheduleGrid.tsx`, `CalendarModal.tsx` | | 담당자 B | 시작 전 |
| 일정 조율/일정 추가/AI 추천 TOP3 | `aiRecommender.ts` + `ScheduleGrid.tsx` | | 담당자 B | 시작 전 |
| 일정 조율/일정 추가/확정 | `ScheduleGrid.tsx` | | 담당자 B | 시작 전 |

## 상태 값

`시작 전` → `진행중` → `리뷰 대기` → `완료`
